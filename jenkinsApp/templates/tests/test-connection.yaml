apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkinsApp.fullname" . }}-test-connection"
  labels:
    {{- include "jenkinsApp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jenkinsApp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
